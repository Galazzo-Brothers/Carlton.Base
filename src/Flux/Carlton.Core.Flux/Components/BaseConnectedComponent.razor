@namespace Carlton.Core.Flux.Components
@typeparam TViewModel
@implements IConnectedComponent<TViewModel>

 @code {
    [Parameter]
    public TViewModel ViewModel { get; set; }
    [Parameter]
    public EventCallback OnComponentEvent { get; init; }
}
