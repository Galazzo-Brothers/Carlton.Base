@namespace Carlton.Core.Layouts.Components

 <span class="horizontal-spacer"></span>

@code {
    [Parameter]
    public int WidthPercentage { get; set; }

    private string SpacerStyle
    {
        get => new StyleBuilder()
                    .AddStyle("--spacer-width", $"{WidthPercentage}%")
                    .Build();
    }
}