@namespace Carlton.Core.Components.Pills

@* Represents a pill component used for displaying text in a pill-like format *@
<div class="pill">@Text</div>

@code {
    /// <summary>
    /// Gets or sets the text to be displayed within the pill.
    /// </summary>
    [Parameter]
    public string Text { get; set; }
}
