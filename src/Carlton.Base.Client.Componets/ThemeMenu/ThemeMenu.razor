@namespace Carlton.Base.Components

<div class="theme-menu" @onclick="()=> IsDarkMode = !IsDarkMode">
    <span class="icon mdi mdi-24px mdi-@(IsDarkMode ? "moon-waxing-cresscent" : "weather-sunny")"></span>
</div>


@code {
    [Parameter]
    public bool IsDarkMode { get; set; }
    //[CascadingParameter]
    //private string Theme { get; set; }

    //[CascadingParameter]
    //private Action<string> UpdateTheme { get; set; }
}
